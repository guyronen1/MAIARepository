> Part of MAIA CLAUDE.md, split out for size. Root index: ../CLAUDE.md

# Known follow-ups (not blocking)

- **🏗️ ARCHITECTURE — heterogeneous scan rules under one MonitoredJob — DECIDED: Tier 2.5, SHIPPED.**
  **Decision (2026-06-09):** build a **`ScanSource`** entity (a typed observation point within a job: its own ScanType + scan config + scan rules), keep **leases 1:1 with MonitoredJob** (per-source leases deferred — operator confirmed no cadence-divergence need: B2B DB+log mix isn't a problem, no differing frequency requirements, no real-time recovery tension). Per-source `ScanRunHistory` rows (achievable under per-job leases). This delivers Option A's "roll up by operational concept, drill down to per-source detail" UX without the per-source lease machinery.
  **Phasing (all phases complete):** **(a)** schema + backfill — `ScanSources` table + `ScanSourceId` on 5 tables. **(b)** worker + `IScanStrategy` contract. **(c)** read-side rollup. **(d)** config UI/UX rework (dedicated per-job screen). **(e/cleanup — DONE 2026-06-17)** `ScanSourceId` enforced NOT NULL on all 5 tables (migration `Tier25CleanupColumns`), ALL 7 legacy `MonitoredJob` scan-config columns dropped including `ScanTypeId` (`LogFolder`, `SearchPatterns`, `ConnectionName`, `LogSourceUrl`, `InputFolder`, `IncludeSubfolders`, `ScanTypeId` — FK and index dropped in the same migration). Dead scan-config fields also removed from `UpsertMonitoredJobRequest` contract (2026-06-20). **Per-source leases remain cleanly deferrable** — re-key the lease FK + claim JOIN as an isolated later change if cadence divergence ever becomes real.
  **UI/UX direction (part of this change, phase d):** the Monitored Jobs config screen is getting too heavy. Keep the **job list** with **edit-job-details in a drawer** (as today). But **"Configure" opens a dedicated full job-configuration screen** (not the current tabbed drawer panel): each section (Scan Sources, Classification Rules, Fix Options) is its own **row panel** showing the relevant list, and **editing an item in a section opens a drawer** (as today). Sources nest their scan rules.
  *(Original three framings + the per-coupling analysis + Tier 1/2/3 trade-offs preserved below for context.)*

  **Root cause (verified in code):** `ScanType` lives at *both* levels and they conflict. `MonitoredJob.ScanTypeId` makes the worker pick exactly one strategy (`strategies.FirstOrDefault(s => s.ScanType == job.ScanType)`), but `ScanCheckRule.CheckType` is *already* a per-rule discriminator and every strategy already filters the job's rules by it (`FileSystem`→`ErrorKeyword`, `Database`→`ColumnRange`/`ValueEquals`, `ApiEndpoint`→`StatusCode`/`ResponseContains`, `FileContent`→`FileContent`). So the rule layer is already type-aware; the job-level `ScanType` is the artificial bottleneck duplicating what the rules know.

  **Concrete coupling points any fix must address:** (a) worker dispatch (`FirstOrDefault(==job.ScanType)` → run every strategy with ≥1 matching active rule); (b) per-type config fields (`LogFolder`/`SearchPatterns`, `ConnectionName`, `LogSourceUrl`) are job-level + singular — all DB rules share one connection, all FS rules one folder; (c) lease duration is per-`ScanType` and doubles as the per-job timeout → mixed job needs `max(involved)` or a job-level value; (d) `ScanRunHistory` is one-row-per-job-scan → aggregate vs per-strategy rows; (e) **FS has a non-rule-driven "full-pipeline over all lines" mode triggered purely by `ScanType=FS` with no keyword rules — that signal vanishes under CheckType dispatch and needs an explicit flag**; (f) UI rule editor branches its form on the *job's* ScanType today → would branch on the *rule's* CheckType.

  **Recommendation (Claude, 2026-06-07):** **Tier 1 now, Tier 3 as the north star; avoid Tier 2.**
    - **Tier 1 (≈ framing 1) — CheckType-driven dispatch.** Worker runs every strategy whose supported CheckTypes appear in the job's active rules; `CheckType` is the discriminator (no new field needed); the config columns already coexist (just fill more of them). Unlocks the operator's exact example *when both tables share one connection*. Smallest, lowest-risk change; matches the additive codebase style. Costs: UI rework (rule editor goes CheckType-first; job config shows the source-config sections actually used), and two decisions — fate of `ScanType`-at-job (remove vs keep-as-default) and how to re-signal FS full-pipeline mode.
    - **Tier 2 — push source config onto `ScanCheckRule` (connection/folder/url per rule).** Unlocks different connections per rule BUT adds yet more nullable columns to `ScanCheckRule`, already the widest table and already flagged at the "normalize?" threshold during FileContent. **A trap** — spends schema churn without giving the clean model.
    - **Tier 3 (≈ framing 2) — `ScanSource` / `MonitoredService` sub-entity.** `MonitoredJob (1) → (N) ScanSource [typed + its own config] → (N) ScanCheckRule`. Job = the logical operational concept; sources = typed observation points (this DB on this connection, that folder, that API); rules = checks within a source. Conceptually correct; resolves the `ScanCheckRule`-width problem (per-type config moves to the source); lease/watermark/history/dashboard all key naturally to a source. Heavy: new table, move `ScanCheckRule.MonitoredJobId → ScanSourceId`, worker rewrite, a Sources UI tab, watermark re-key, data backfill (each existing job → one source).
    - Framing 3 (naming conventions / N jobs per process) is the current workaround the operator wants to escape — fragments the mental model, splits leases/failures/recommendations/views.

  **Decisions that pick the tier:** Is the real "two tables" case same connection or different? (same → Tier 1 suffices; different → go straight to Tier 3, don't pass through Tier 2.) Does one job ever need two databases / two log folders? Is "one business process = one monitored job" the intended operator mental model? When ready, resolve these, then plan the chosen tier end-to-end.

  **Phase (b) decisions (confirmed 2026-06-09, implement in the worker round):** `IScanStrategy.ScanAsync(MonitoredJob job, ScanSource source)` — job carries identity (`JobTypeId`/`MonitoredJobId`/`Name`), source carries the 8 config fields + its rules. Worker runs sources **sequentially** under the per-job lease, **one `ScanRunHistory` row per source** (`MonitoredJobId` + `ScanSourceId`). Source **exception** → best-effort (that source's row = Failed, continue); **timeout** (shared `jobCts`) → ends the tick, remaining sources get **no row** (history = actual executions only). Rolled-up lease outcome = worst wins (`Timeout > Failed > Success`), **no `Partial`**. Lease-duration claim stays `job.ScanTypeId`-based through (b)/(c); MAX-vs-SUM-vs-per-source-CTS decided in (d) (MAX is likely *wrong* under sequential execution — sum risk). Interface change is **atomic across all 4 strategies** → one lockstep step (no bridge), since only FileContent has unit tests.

- **Per-rule scan resilience — DONE across all scan strategies (completed 2026-06-15).** The latent bug (one throw mid-loop aborts the whole source scan and orphans the failures earlier items already created, because `classify`/`suggest` run only *after* the loop → source fails every tick) is now closed everywhere via one pattern: wrap each item (rule/file/pattern), log + remember the first error, classify/suggest whatever was created, then rethrow so the scan-run is still recorded Failed (visible). Per strategy: **DatabaseScanStrategy** — per-rule (2026-06-14). **FileSystemScanStrategy** — per-file (keyword mode, the `IOException`-on-a-locked/rotating-log trigger) + per-pattern (full-pipeline mode); first unit coverage (`FileSystemScanStrategyTests`, 3 tests). **FileContentScanStrategy** — per-file try/catch wrapping the file body, keeping the existing inner `FileContentTooLargeException` catch as the *expected* per-rule skip (oversize is counted, not an error); `FileContentScanStrategyTests` gained a resilience test (one file's faulted `SaveAsync` doesn't stop the other from being created+classified, scan rethrows). **ApiEndpointScanStrategy** — structurally immune (single check → one failure → classify inline; already wraps the HTTP call). All three fixed strategies share the same shape; future strategies should follow it. (230 tests total at that point; ApiEndpoint unit tests added separately 2026-06-20, see follow-up.)
- ~~**Three of four scan strategies (FS, DB, ApiEndpoint) have no direct unit test coverage.**~~ **ApiEndpoint — DONE 2026-06-20:** `ApiEndpointScanStrategyTests` added (14 tests covering happy path, non-2xx status, body-keyword variants, HTTP exception → Unreachable, body truncation at 500 chars, failure-field population, classify/suggest wiring, and the no-loop guarantee). **DB — still open:** only integration-level coverage; worth adding when `DatabaseScanStrategy` next changes meaningfully.
- **Four bespoke drawers in the monitored-jobs flow (job-edit, scan-rule, class-rule, fix-rule) don't use the shared `DrawerComponent`.** Functional today; convert opportunistically when one needs significant changes anyway (not a side-effect of the Tier 2.5 structural migration).
- **`CheckType.StatusCode` / `CheckType.ResponseContains` are defined in the enum but consumed by no strategy.** `ApiEndpointScanStrategy` uses a hardcoded heuristic (non-2xx OR body contains error/exception/failed) and iterates no `ScanCheckRules`. So ApiEndpoint sources are rule-free by design. If configurable API checks are ever wanted (status-equals, body-contains, JSON-path predicate), implement those CheckTypes in the strategy first — then the Sources UI can show nested rules for API like the other types. Until then the UI shows an API source as URL + "no rules needed".

- ~~**Operator identity is hardcoded `'operator'`**~~ **RESOLVED 2026-06-19** — `operatorId` removed from all contracts; `AuditLog.Actor` / `OperatorAction.OperatorId` now come from the authenticated cookie principal (`ICurrentUserAccessor`).
- ~~**No authn/authz on any controller.**~~ **RESOLVED 2026-06-19** — Auth v1 Phases 0–4 fully shipped (MAIA-local login, httpOnly server-side sessions, three role tiers enforced per-endpoint with a default-closed fallback, Users admin screen at `/config/users` with list/create/edit/reset-password, forced password rotation). Remaining auth work: AD authn front-end (v2), audit-viewing UI, self-service email password reset (v2). `UserSessions` table held 2 rows as of 2026-06-20 — no sweep worker needed yet.
- **Analytics endpoint response shapes are inconsistent — LEFT AS-IS (reviewed 2026-07-18).** `failures-over-time` wraps its rows in `{ range, bucketSize, rangeStart, rangeEnd, buckets[] }`; the newer `failures-by-job` and `resolution-mix` return plain arrays. **Decision: don't flatten.** The wrapper's `bucketSize` is consumed by `errors-over-time-chart.component` for hour-vs-day axis-label formatting; flattening to a bare array would push bucket-granularity inference onto the client for no functional gain — exactly the "refactor purely for consistency" this item warned against. Revisit only if the chart stops needing the metadata.
- ~~**No audit-viewing UI yet.**~~ **SHIPPED 2026-06-20** — global `/config/audit` screen, Admin-only. Paged feed with EntityType/Actor/EventType/date-range filters, event-type badge colouring, and `→` diff highlighting. Per-entity contextual tabs remain deferred (no concrete operator ask yet).
- **`DbFixHandler.HandleAsync` — honest message SHIPPED 2026-07-18 (real remediation still deferred).** Was a TODO stub logging a misleading "Attempting DB connectivity fix" then returning `false`. Now it logs a clear, actionable **Warning** ("no automated DbFix handler / no FixPolicyRule configured for this (JobType, ErrorType) — routing to ManualRequired; configure a SqlScript/StoredProcedure policy to auto-remediate") and still returns `false`, which correctly routes the failure to `ManualRequired`. No speculative DB-reconnect logic was invented. When a real DbFix remediation is built it should reuse `ISqlFixScopeValidator` for any write (comment in the handler records this).
  - **The `{sourceId}`-in-WHERE write-guard is now SHIPPED for SqlScript fix payloads (2026-06-11)** — see the SqlScript write-guard decision below. When `DbFixHandler` proper is implemented it should reuse the same `ISqlFixScopeValidator`. `SourceId` is the natural key into the operator's table (invoice/order id) — what a fix's WHERE must target; `FailureId` is MAIA's internal PK and meaningless as a fix target (and deliberately does NOT satisfy the guard).
- **Frontend `monitored-jobs.service.ts` is GET-only — LEFT AS-IS (reviewed 2026-07-18).** It's a clean single-method service (`getAll()` → `/api/data/monitored-jobs`) genuinely consumed by `search.service` + `scan-jobs.component` — a *different* endpoint from `config.service.getAllJobs()` (`/api/config/monitored-jobs`, config-with-rules). Not dead, not duplicated; "consolidating" would churn two working features for no functional gain. Kept.
- ~~**`ILogReader` / `FileLogReader` are no longer consumed.**~~ **REMOVED 2026-07-18.** Interface, `FileLogReader` impl, and the DI registration deleted; the classifier reads `JobFailure.ErrorMessage`. The one remaining reference was a dead local in `PipelineIntegrationTests.BuildPipeline` (constructed but never passed to anything) — removed too. 288 tests green.
- **Sort UI on failures-list.** None today. URL hygiene reserves the slot but no UI is wired up.
- **Style budget warnings.** `features/dashboard/dashboard.component.ts` (~4.79kB vs 4kB budget) and `features/config/monitored-jobs/monitored-jobs.component.ts` (~4.34kB) exceed the Angular per-component SCSS budget. Pre-existing pattern in this codebase, not regressions from current work; raise the budget or extract shared styles.
- ~~**No DB-level CHECK constraints for composite invariants.**~~ **SHIPPED 2026-07-18** — migration `AddCompositeInvariantCheckConstraints` adds three CHECK constraints (defense-in-depth for what `FixPolicyConfigController.ValidateCompositePayload` enforces): `CK_FixPolicyRuleSteps_ActionType_NotManualOrComposite` (a step can't be Manual/Composite), `CK_FixPolicyRuleSteps_ActionPayload_NotEmpty` (non-blank step payload), `CK_FixPolicyRules_CompositeHeader_NullPayload` (a Composite header carries no payload). `ActionType` is stored as its enum name (`HasConversion<string>`), so the checks compare string names. Verified: applies cleanly to existing data + a `Manual`-step INSERT is rejected. **Deploy note:** apply via `dotnet ef migrations script`/`database update` against a DB with contiguous migration history — the local dev DBs (`MaiaDB` has no history; `MaiaDb_Test` has an unrelated `ScanSources.LogFolder` model-vs-schema drift) can't take a clean `database update`.
- **`FixExecutionLog` has no retention worker.** Table sits at ~12k rows today and grows monotonically. Composite policies write one row per step; high-frequency drains amplify it further. Mirror `ScanHistoryRetentionWorker` with a configurable retention window (default 90d) when this becomes a noise / size problem.
- **Composite scan rules (deferred from FileContent v1).** Operator requested multi-step scan checks for both file-content (multi-predicate within one file) and DB scans (cross-table value checks). **The cross-table DB case is now served by `CheckType.SqlQuery`** (see the SqlQuery decision below) — the operator writes the JOIN/aggregation directly, so explicit composite UI is only needed if a concrete multi-predicate file-content case appears. v1 supports single-predicate / single-check rules; operators express compound checks via XPath compound conditions (XML), a JOIN in `SourceTable` (DB ColumnRange/ValueEquals), or a full SqlQuery. When concrete cases demand explicit composite UI, mirror the `FixPolicyRule` composite pattern: a parent `ScanCheckRule` with `ActionType=Composite` + a child `ScanCheckRuleStep` table (`StepId`, `RuleId`, `Locator`/`TargetColumn`, `PredicateType`, `PredicateValue`, `StepOrder`). All steps evaluated, AND logic, fires a single `JobFailure` on full match. Architecture supports it additively — single-predicate v1 rules keep working unchanged.
- **FileContent v2 backlog.** CSV/JSON/Excel extractors (add a `FileFormat` value + an `IFileContentExtractor` impl); content-hash watermarking (mtime is the v1 dedup key); per-rule failure-mode config (malformed-file behavior is fixed at "log Warning, skip" in v1); regex predicate type; streaming extraction for >5MB files; walk-once-apply-many is already implemented so multi-rule scans don't re-read files.
- **Invalid locator / extraction failure visibility — SHIPPED (2026-06-09).** Both halves done. **(a)** `IFileContentExtractor.ValidateLocator(locator)` (XML impl compiles the XPath via `XPathExpression.Compile`) — `ConfigController` injects `IEnumerable<IFileContentExtractor>` and validates `ExtractorLocator`/`IdentifierLocator` at save, returning `400 {error:"InvalidLocator"}` on a malformed XPath (the `\\`-vs-`//` typo from the 2026-06-08 incident is now rejected at save). **(b)** new `PredicateUnevaluableSkips` counter (mirrors `IdentifierExtractionFailures`/`OversizeFileSkips`) incremented when a predicate is set but the locator yields no value, flowed `ScanResult → worker → ScanRunHistory` (migration `AddPredicateUnevaluableSkips`) and — since the prior two counters were DB-only — **all three are now exposed in `ScanRunDto`** (`GET /api/data/scan-runs`). The extractor still returns null on bad XPath at scan time (don't crash a scan over one rule) — but a bad locator can no longer be *saved*, and a valid-but-unmatched locator is now counted. Remaining visibility nicety (not built): a dedicated scan-run-history UI table to show these counts (the `/scan-runs` endpoint exposes them, but no frontend view consumes it yet).
- **Unconfigured surface — SHIPPED (2026-06-06), with one deferral.** The `/unconfigured` screen + dashboard tile now surface both coverage gaps (Case A unclassified clusters, Case B missing-policy). **Deferred (investigated 2026-06-20):** Case B is a gap detector, not a suggestion engine — no cluster analysis runs, so there is no meaningful `SuggestedBy` value (unlike Case A's "ngram-v1" tag). Wiring a hardcoded `"system:policy-gap"` label through the deep-link would add plumbing with no ML/training-data value. Revisit when Case B gains a real recommender. The backend `UpsertFixPolicyRuleRequest` already accepts all three provenance fields; the frontend just omits them. Note: the deep-link target was updated to `/config/monitored-jobs/:id?errorTypeId=` in the Tier 2.5 d2 rework (the `fixForJob` param and `MonitoredJobsComponent` path documented below are stale — see the Case B deep-link decision for the live path). Case A creates a JobType-level ClassificationRule which, under the new UNION classifier semantics, correctly applies to all jobs of that type; a "link to just this job" option remains a possible future enhancement for narrower scope.
- **Failure drawer composite step list re-fetches `FixPolicyRule.Steps` on every 5s poll tick** (per composite rec). Cheap today (clustered PK lookup, typically 1-3 composite recs per drawer-open), but the cost scales with rec count. If a failure ever has many composite recs, switch to a single batched fetch (one query for all distinct ruleIds on the failure) and cache on the rec object.

---

# Full-codebase audit — gaps and problems, ranked (2026-07-08)

Findings from a docs-vs-code + architecture audit of both `Maia.Services` and `Maia.Client`. Doc accuracy verdict: MD files are highly accurate; the items below are the residual gaps. Items already tracked above are cross-referenced, not restated.

**🔴 1. Second-order SQL injection in the fix path — RESOLVED 2026-07-17.** Was: `SqlScriptExecutor.cs:74` substituted `{sourceId}`/`{referenceId}` into the SQL as a **plain string** (`PlaceholderResolver` did text `Replace`, no parameterization) then ran `new SqlCommand(sql, conn)` with **no parameters**. The ScriptDom write-guard (`SqlFixScopeValidator`) validates the placeholder is *positioned* inside the WHERE — it did not defend the *value*. `SourceId` originates from scanned data, so a row whose id is `' OR '1'='1` turned a scoped `WHERE id='{sourceId}'` into a bulk mutation under a write-capable login. **Fix (parameterized, not escaped):** new `IPlaceholderResolver.ResolveSqlAsync` → `ParameterizedSql` (rewritten SQL + `@pN` values). Every recognised `{token}` becomes a `@pN` marker and its value is bound as a `SqlParameter`; scanned data never becomes SQL text. Operator's quoted `'{sourceId}'` (and the UI helper's appended `= '{sourceId}'`) collapse to a bare `@p0` (matched quote-pair consumed); lone/embedded quotes degrade to an inert literal (0 rows), never inject or unbalance. Composite SqlScript steps route through the same `SqlScriptExecutor`, so they're covered too. `StoredProcedureExecutor` was already parameterized (`@FailureId` only, no template interpolation); `DbFixHandler` is a no-op stub. The write-guard now defends *position*, parameterization defends *value* — both required. 7 new tests in `PlaceholderResolverTests` (incl. the `' OR '1'='1` regression). See the DECISIONS.md addendum under "SqlScript fix write-guard".

**🔴 2. Orphaned-unclassified-failure crash window — RESOLVED 2026-07-18.** Was: FS and DB scan strategies advance watermarks *inside* the scan loop but run classify/suggest only *after* the whole loop, so a crash/timeout between the two left failures saved with the watermark advanced past them — never re-read, never re-classified (only the manual `classify-pending` endpoint covered it). **Fix:** a safety-net sweep in `MonitoringWorker`. New `IReclassifyOrphanedFailuresUseCase` (`ReclassifyOrphanedFailuresUseCase`, Maia.Application) queries `Status=Failed AND ErrorTypeId IS NULL AND DetectedAt < cutoff` (new `IJobRepository.GetUnclassifiedOlderThanAsync`) and runs the same classify→suggest the strategy would have. The worker runs it **once at startup** (force past throttle — recovers orphans a prior crash left) and **throttled each tick** (even on ticks that claim nothing — another instance may have crashed). One config knob `Scan:OrphanReclassifyMinutes` (default 10, min 1) drives both the **age gate** (a failure must be older than this to be swept, so genuinely in-flight scans aren't touched) and the **cadence**. Concurrency-safe: re-classifying sets the same `ErrorTypeId` and `GenerateSuggestionsUseCase` already skips failures with an existing recommendation (`ExistsForFailureAsync`) — so overlapping an in-flight scan can't duplicate. Failures matching no classification rule stay unclassified and are retried next sweep (same as the manual endpoint; cheap). 4 new tests (`ReclassifyOrphanedFailuresUseCaseTests`). Reorder-in-strategy was rejected: it wouldn't cover a timeout mid-file and would have to be duplicated per strategy; the sweep is one place, robust to any crash cause, and covers future strategies.

**🔴 3. Frontend tests — FOUNDATION LAID 2026-07-18 (ongoing).** Was zero `.spec.ts` despite Karma/Jasmine configured. **Bootstrapped:** confirmed the `@angular/build:karma` runner works headless (`npm run test:ci` = `ng test --no-watch --browsers=ChromeHeadless`; needs Chrome — set `CHROME_BIN` if not auto-detected), and wrote the first two specs establishing both patterns: **(a) pure-function** — `coverage-match.util.spec.ts` (16 cases: effective-rule union, `Field=Value` exact-match nuance, SqlQuery not-determinable, override-wins fix resolution); **(b) component-with-TestBed** — `fix-option-drawer.component.spec.ts` (9 cases: execution-type→category derivation, Manual/Composite payload+step clearing, the SqlScript "scope to failing row" shortcut, composite step re-ordering) — instantiated via `TestBed` with `provideHttpClient`/`provideHttpClientTesting`/`provideRouter`, testing methods without rendering. Both deliberately cover the highest-risk task-4b (drawer decomposition) code. **Expanded to 5 spec files / 37 tests:** added `worker-status.service.spec.ts` (fakeAsync + `HttpTestingController` — slice isolation, keep-last-value + isStale on error, skip-while-pending, refcounted start/stop, PagedResult unwrap), `failure-detail.stages.spec.ts` (the documented alternative-middle-stage invariant: Acknowledged vs Manual rendered exclusively, `isStageCompleted` derives order from the rendered stages), and `scan-rule-drawer.component.spec.ts` (ScanType-driven form defaulting + the SqlQuery no-WHERE warning). Pattern for TestBed method-tests: `createComponent` without `detectChanges` so constructor effects / `ngOnInit` polling don't fire. **Now 6 spec files / 46 tests — all four audit-named high-risk units covered:** added `failures-list.url-state.spec.ts` (mocked `ActivatedRoute.queryParamMap` + spied `Router.navigate`; `ngOnInit()` called directly so the template/drawer never render — pins inbound param→signal mapping incl. edge normalization, outbound `patchUrl` default-stripping, sort-flip, client-side filter, nav computeds). Gotcha captured: a bare `Router` stub needs an `events` observable because `DrawerComponent` transitively instantiates `NavigationHistoryService` (or just use `provideRouter([])` — a real router has `events`). **Now 9 spec files / 57 tests — all four audit-named units + ALL FIVE task-4b drawers covered:** added `edit-job` (form build + jobType resolution + save-validation + PUT-emits-saved), `scan-source` (isFileBased/scanTypeName, create-vs-edit open, blank-name guard), and `class-rule` (openNew prefill, openEdit errorType-code resolution, save guard, and `openLink` → GET → filters already-linked + re-emits `allClassRulesRefreshed`). **Still to cover (truly incremental now):** deeper `failure-detail` approve/reject/retry flows and other feature components (dashboard, unconfigured, operator-actions, config list screens).

**🔴 4. No production environment config — RESOLVED (verified 2026-07-18).** (Audit note was stale — fixed alongside the IIS deployment work.) `environment.prod.ts` exists with a **root-relative** `apiUrl: '/Maia.API/api'` (`production: true`), and `angular.json`'s `production` configuration (the `defaultConfiguration`, so plain `ng build` uses it) has the `environment.ts`→`environment.prod.ts` `fileReplacements` + `baseHref: '/Maia.Client/'`. Root-relative + same IIS site = same-origin (no CORS, `SameSite=Strict` cookie works) on any host with no rebuild. Verified: a production `ng build` bakes `/Maia.API/api` into the bundle with **zero** `localhost:5095` references and emits `<base href="/Maia.Client/">`. Matches the DEPLOYMENT.md topology.

**🟡 5. API layer leaks Infrastructure — write paths RESOLVED 2026-07-18 (read side decided + documented).** Was: five controllers queried EF inline via `IDbContextFactory<MaiaDbContext>`, including *writes*. **Decision (see DECISIONS "API read-side DbContextFactory is sanctioned CQRS-lite"):** controller **reads** via the factory are sanctioned (query-shaping stays near the endpoint, `AsNoTracking()`); **writes** must go behind a Core interface. **Applied:** `UsersController` moved fully off the factory onto `IUserRepository` (added `ListAsync`/`UsernameExistsAsync`/`AddAsync`/`CountActiveAdminsExceptAsync`); `RecommendationsController.Retry`'s re-arm write moved to `IRecommendationRepository.ReArmForRetryAsync` (rec + failure flipped to Failed in one transaction), its remaining reads now `AsNoTracking()`. 2 new tests (`SqlRecommendationRepositoryReArmTests`). **Still open:** `ConfigController`'s inline EF writes — deferred to its per-entity decomposition (audit #6); the same rule applies when that lands.

**🟡 6. God classes — DONE 2026-07-18 (backend + frontend).** **Backend:** `ConfigController.cs` (1,692 lines) split per entity into `Controllers/Config/`: `ConfigControllerBase` (shared audit helpers — `WriteAuditAsync`/`BuildDiff`/`FormatValue`/`Actor`), then `ErrorTypesConfigController`, `MonitoredJobsConfigController`, `ScanSourcesConfigController`, `ScanRulesConfigController`, `FixPolicyConfigController`, `ClassificationRulesConfigController` — each `[Route("api/config")]` so all 30 routes are byte-for-byte preserved (verified by the authorization matrix + `ScanSourceCrudTests`, retargeted to the split controllers). Request contracts → `Config/ConfigContracts.cs` (same `Maia.API.Controllers` namespace). Inline EF stayed as-is (still the ConfigController-family exception to the write-behind-interface rule; a repository pass is a separate future item). **Frontend:** `job-config.component.ts` (1,804 lines) → **688-line shell** (3 panels + coverage markers + orchestration) + five standalone drawer children under `monitored-jobs/drawers/`: `EditJobDrawerComponent`, `ScanSourceDrawerComponent`, `ScanRuleDrawerComponent`, `ClassRuleDrawerComponent` (editor + link picker), `FixOptionDrawerComponent`. Each owns its form + validation + ConfigService save, exposes a public `open*(…)`, and emits `(saved)` → parent `reload()`; the parent holds them via `viewChild` and its `open*` methods delegate. `ClassRuleDrawerComponent` re-emits its fetched rule list via `(allClassRulesRefreshed)` so the parent's shared `allClassRules` (which feeds `effectiveClassRules`) stays in sync as before. The two unused `job`-param hints were cleaned in passing (`applyFixDeepLink`'s unused param; `openFixRuleDrawerForClassRule`'s unused `const job`). Verified by `ng build` (AOT template + type check; frontend still has no unit tests — audit #3). No behavior change intended.

**🟡 7. Silent frontend error handling — read-load failures RESOLVED 2026-07-18.** The pre-existing `NotificationService` (+ ShellComponent toast stack) was only wired to the auth interceptor (401/403). Now every **one-shot read-load** failure surfaces a toast via `notify.error(...)` instead of silently resetting a loading flag: failures-list (page load + adjacent-page nav), failure-detail (initial load only), and the config/list screens (classification-rules, error-types, monitored-jobs list, job-config, operator-actions, recommendations, scan-jobs). **Deliberately NOT toasted** (would spam a toast every 5s during an outage): the **polled** endpoints — `WorkerStatusService` (self-handles via `isStale`) and `failure-detail`'s silent poll refreshes (`opts.silent`). **A global HTTP error interceptor was rejected** for exactly that reason (can't distinguish polled from one-shot without a fragile URL skip-list). Left already-handled callbacks alone (users banner, job-flow error state, login/change-password/save-conflict inline feedback). Pinned by a new `failures-list.url-state` test (GET error → `notify.error` called, spinner cleared). **Deferred (thread a — per-panel staleness rendering):** the top-bar heartbeat chip (UI/UX item 2, shipped) already renders the shared `isStale` state; per-panel dashboard staleness off the same `PolledData` flags remains a possible future polish, not done here.

**🟢 8. Smaller items (verified still true / newly noted):**
- Already tracked above and re-verified open: `DbFixHandler` TODO stub, unused `ILogReader`/`FileLogReader` registration, unconsumed `StatusCode`/`ResponseContains` CheckTypes, no `FixExecutionLog` retention worker, no DB CHECK constraints for composite invariants, `monitored-jobs.service.ts` GET-only.
- ~~DB-scan SQL brackets column names as `[{name}]` without `]`-escaping.~~ **FIXED 2026-07-18** — new `Bracket(identifier)` helper in `DatabaseScanStrategy` escapes `]`→`]]` and now wraps every SQL identifier (SELECT columns, watermark filter, ORDER BY, `QuoteTable`, `QuoteColumnRef`, `QueryFilteredMaxAsync`, `BuildFilterClause`). The `[Field=Value]` classifier tokens in failure MESSAGES are display-only (not SQL) and left as-is. Values were already parameterised; the identifier was the one unescaped thing.
- ~~Connection strings committed in `appsettings.json`; move server secrets out.~~ **ADDRESSED 2026-07-18** — the checked-in `appsettings.json` holds only non-secret localdb dev placeholders (Trusted_Connection, no passwords). `appsettings.Production.json` (the real per-host creds) is now **gitignored** so it can't be accidentally committed (was untracked-but-not-ignored — a latent leak). Matches DEPLOYMENT.md's "set the real connection string on the server, not in the repo."
- Dead code: `MonitoredJob.ScanCheckRules` navigation — **left in place (not actually dead):** it's still consumed by `SqlMonitoredJobRepository`'s `Include` and `MonitoredJobDto`. Removing it is an EF model change (relationship config) → migration + regression risk, not worth it for a 🟢. (`lastScanLabel()` / `withActor()`/`actorParams()` were already removed in the UI dead-code pass.)
- ~~Stale "Phase 1 authn-open" comments.~~ **FIXED 2026-07-18** — `MaiaSessionAuthenticationHandler` + `Program.cs` comments now state the accurate posture: the handler does authentication only (anonymous on missing/invalid, never rejects); enforcement is LIVE at the authorization layer (fallback `RequireAuthenticatedUser` + per-endpoint role policies).
- ~~Doc drift.~~ **FIXED 2026-07-18** — empty `Maia.Services/CLAUDE.md` deleted; DECISIONS Serilog note corrected (path is absolute `c:/logs/maia-api-.log`, not relative); root CLAUDE.md's spec-PDF reference de-pinned to "the MAIA specification PDF (under `docs/`)" since `docs/` is being reorganized.
- Undocumented frontend surface: three dashboard charts (errors-over-time, failures-by-job, resolution-mix — only the first is documented), `unconfigured.service.ts` / `users.service.ts`, the drawer's drill-in/browse back-button heuristic.

---

# UI/UX to-do list (agreed 2026-07-11 — handle one by one, in order)

From the design/UX review; all items operator-approved. Items marked **[discuss first]** need a design conversation before implementation.

1. ~~**Split Recommendations vs Operator Actions (currently the same component).**~~ **SHIPPED 2026-07-16.** `/operator-actions` is now a dedicated `OperatorActionsComponent` — a decision-history view (one row per Approve/Reject/Retry, newest first, with operator, decision badge, joined failure/job/rec context, executed + current-status outcome, failure drawer via `?selected`, server-side text/decision filters + paging). Backed by new `GET /api/data/operator-actions` (DataController, User tier; OperatorActions → AIRecommendations → JobFailures join, `OperatorActionDto`). `RecommendationsComponent` lost its vestigial `isOperatorMode` branch and stays the pending-action queue (its state filter still offers Pending Review / Approved / Rejected / Executed). Covered by `OperatorActionHistoryTests` (4 tests) + a new authorization-matrix row.
2. ~~**Surface the staleness already tracked.**~~ **SHIPPED 2026-07-16.** The top bar now renders an amber "⚠ Reconnecting… · Xs ago" chip (driven by the `status$` PolledData `isStale`/`lastUpdatedAt`/`lastError`) whenever the shared 5s worker-status heartbeat fails while serving a cached snapshot; the chip's tooltip carries the error + last-good age. The worker pill also dims and stops its green pulse while stale, so it can't falsely read "Live" during a disconnect. Shown only when stale (fresh state stays uncluttered) and only after a first success (`value !== null` — before that it's "connecting", not "reconnecting"). The `WorkerStatusService` plumbing was already correct; this is the render. Per-panel dashboard staleness off the same flags remains a possible future polish (top-bar heartbeat covers the global "data frozen" case since all four slices poll together).
3. ~~**Consolidate the "something is happening" signals.**~~ **SHIPPED 2026-07-16.** The top-bar worker pill is now the pause/resume control (Admin-gated: renders as a `<button>` that toggles `POST /api/admin/worker/pause|resume` via `ScanService`, with a spinner while toggling and a "click to pause/resume" tooltip hint; non-admins see the same pill read-only). The duplicate dashboard Live/Paused pill + its `togglePause`/`canControlWorker`/`isSystemPaused`/`pauseToggling` logic and CSS were removed. The top bar also became the always-on polling owner in the same area (see the gray-badge bug fix). Remaining "activity" surfaces — the dashboard inline activity strip and the scan-result toast — are out of scope here; the strip is item 10.
4. ~~**Dashboard visual hierarchy.**~~ **SHIPPED 2026-07-16.** All six KPIs stay in the row, but **Resolved Today** — the only pure-reassurance metric (no action) — is de-emphasized so it doesn't compete with the alarm tiles: moved to last position, given a narrower grid column (`.kpi-grid` = `repeat(5,1fr) 0.7fr`) and a `.compact` variant (smaller value/icon, muted `--surface-2` fill). The five action tiles (Active, Awaiting Action, Manual Required, Fix Failures Today, Unconfigured) keep full weight; Fix Failures Today keeps its alarm-pulse + thicker border. (An earlier attempt pulled Resolved Today out into a separate strip below the grid; reverted per operator preference — keep it a tile, just smaller.)
5. ~~**Dark theme.**~~ **SHIPPED 2026-07-16 (both OS-default + manual toggle).** `styles.scss` gained a `maia-dark` SCSS mixin (dark values for every chrome + semantic token; NESS orange kept, nudged brighter) applied via two paths that can't drift: `@media (prefers-color-scheme: dark) { :root:not([data-theme]) }` (follow OS when unset) and `:root[data-theme="dark"]` (explicit override). `color-scheme` set so native controls/scrollbars follow. New `ThemeService` (`core/services/theme.service.ts`): `mode` signal (`system`|`light`|`dark`, persisted to `localStorage['maia-theme']`), reactive to OS changes while in `system`, stamps/removes `data-theme` on `<html>`; SSR-guarded (`typeof window/document`). Anti-FOUC inline `<script>` in `index.html` applies the stored choice before Angular boots. Top-bar sun/moon toggle (`theme.toggle()`, icon = current theme). Because surfaces/text/borders were already token-driven, the flip covers the whole app. Follow-up fixes after live dark-mode testing: (a) the global data-table row-hover was hardcoded `#fafbfc` → white-on-white on dark; changed to `var(--surface-2)`. (b) A dedicated **warn-panel token set** (`--warn-bg`/`--warn-bg-2`/`--warn-border`/`--warn-strong`/`--warn-text`; light = prior hardcodes, dark = amber-on-dark) now backs every amber warning surface — the Flow view's no-classification row tint + gap chip, the coverage-gap ⚠ pills + collapsed-source rollup chip, the soft-warn / auto-heal / dup-warn panels (job-config, classification-rules, unconfigured), and the top-bar **Paused** pill — so they read correctly in both themes instead of light-amber islands on dark. `.badge-muted` chips tokenized too. **Remaining polish (not done):** the Admin-only `/config/audit` event-badge pastels (`.ev-*`) stay light on dark — internally consistent + readable, low traffic. Decorative orange gradient chips (`op-chip`/`ai-chip`, white text) are intentionally left.
6. ~~**Global search / jump-to.**~~ **SHIPPED 2026-07-16.** `Ctrl/Cmd+K` command palette (+ a top-bar Search trigger with a platform-aware hint). New `SearchService.query(text)` returns a role-filtered result list across three sources: navigation destinations (mirrors the side-menu, gated by `hasAtLeast(minRole)`), **failure-by-id** (`1622`/`#1622` → `/failures?selected=`), and **monitored jobs by name** (Operator+ → job config). `CommandPaletteComponent` (`shared/command-palette/`) is a token-styled overlay (works in dark mode) mounted once in the shell; global `Ctrl/Cmd+K` toggle, `↑/↓/Enter/Esc` + click, backdrop-close, jobs lazy-loaded on first open. **Built forward-compatible with the planned LLM assistant** (see AI_INTEGRATION): `query()` is a pure, reusable "navigate" tool and the palette is designed to host a future natural-language "Ask" mode rather than be a competing surface — so it doesn't duplicate the Track-1 config assistant.
7. ~~**Sort UI on the failures list.**~~ **SHIPPED 2026-07-16.** Server-side sort (paging is server-side, so sorting must be too). `SqlJobRepository.GetPagedAsync` gained `sort`/`dir` params with a **whitelisted** column→expression map (`id | job | errortype | detected | status`; unknown → `detected`), a `FailureId` tiebreaker for deterministic paging, and no dynamic property strings (nothing operator-supplied reaches the SQL identifier). Threaded through `IJobRepository` → `GET /api/data/failures?sort=&dir=` → `FailuresService.getFailures`. Frontend: clickable `<th>` headers on the five sortable columns with ▲/▼ indicator + `aria-sort`; click a column to sort (its sensible default dir — dates/ids desc, text asc), click again to flip. Round-trips through the URL (`?sort`/`?dir`, defaults `detected`/`desc` stripped) like the other filters, and applies to cross-page ↑/↓ nav too. 6 new repo tests (`FailuresSortTests`); 275 total green.
8. **[discuss first] Easy scan-configuration creation flow.** Beyond empty-state guidance: operator wants a simple guided flow (wizard-like) to configure a scan easily — job → source → rules in one assisted path instead of the current multi-step drawer sequence. Design discussion pending before build.
9. ~~**Dead code removal.**~~ **SHIPPED 2026-07-16.** Removed: `lastScanLabel()` (dashboard, unused); the no-op `withActor()`/`actorParams()` pass-throughs in `config.service` (inlined at ~20 call sites — actor is server-derived from the cookie, so they did nothing); `ACTION_TYPES` const + deprecated `defaultActionTypeFor()` (job-config); unused `ScanCheckRule` import (config.service) and `ViewChild` import (failures-list). The duplicate `/operator-actions` route folded into item 1. Left: two unused `job`-param hints deep in the 1800-line job-config god component — better cleaned when that component is decomposed (its own future item), not worth churning the big file now.
10. ~~**Simplify the dashboard activity banner.**~~ **SHIPPED 2026-07-16.** The 30s-auto-dismiss + hover-pause + full-restart-on-mouseleave + manual-✕ machinery is gone. Now two simple states: the **in-flight** strip binds directly to live worker state (spinner; appears/disappears on its own, no timer), and a **completed** beat is a plain self-dismissing toast — set when a new completed scan lands (snapshots the label), cleared by one `setTimeout` after 8s. Removed `showActivity`/`bannerCollapsed`/`bannerDismissed`, the mouse-enter/leave handlers, `dismissBanner`, `scheduleDismiss`/`clearDismissTimer`, and the `.banner-dismiss` style; net ~55 lines lighter.
8 & 11 are **DEFERRED** (operator decision 2026-07-16) — revisit later.

11. **[DEFERRED] RTL / Hebrew support beyond `dir="auto"`.** Today only user-data text gets `dir="auto"`. Requirement: real `dir="rtl"` support for Hebrew — potentially the whole layout mirrors (screens, titles, sidebar, drawers swap sides), not just text direction. Scope + approach (per-user language setting? mirror everything vs text-only? translation library — ngx-translate/transloco vs `@angular/localize`?) to be discussed before implementation.
    - **Groundwork SHIPPED 2026-07-16:** a dependency-free `LanguageService` (`core/services/language.service.ts`, mirrors `ThemeService`: `current` signal, `localStorage['maia-lang']`, stamps `lang`/`dir` on `<html>`) + a top-bar globe language switcher. English is the only **enabled** language; Hebrew is listed as "soon" (disabled) with `dir:'rtl'` already declared on its option. When item 11 is picked up, wiring translations + flipping `he.enabled=true` is the change — the switcher UI + `dir` plumbing already exist. No translation layer added yet (that IS item 11).

